class Bandeja {
    constructor(id, temperatura, humedad, cantidadProcesos, estatus) {
      (this.id = id),
        (this.temperatura = temperatura),
        (this.humedad = humedad),
        (this.cantidadProcesos = cantidadProcesos),
        (this.estatus = estatus);
    }
  }
  
  export default Bandeja;